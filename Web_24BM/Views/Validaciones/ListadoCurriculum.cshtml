@model List<Contacto>

<section class="container mx-auto my-5">
     <article class="row">
        
        @if (TempData["Eliminado"] != null)
        {
            @if ((bool)TempData["Eliminado"])
            {
                <div class="alert alert-success" role="alert">
                    Eliminado correctamente
                </div>
            }
        }

        @if (TempData["Actualizado"] != null)
        {
            @if ((bool)TempData["Actualizado"])
            {
                <div class="alert alert-success" role="alert">
                    Actualizado correctamente
                </div>
            }
        }
        
        
        <div class="col-12 mb-5">
            <a class="btn btn-primary" href="/Validaciones">Crear Curriculum</a>
        </div>

        <div class="card">
            <div class="card-body"> 
                  <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Apellidos</th>
                    <th scope="col">Email</th>
                    <th scope="col"> Eliminar</th>
                    <th scope="col"> Actualizar</th>
                    @*<th scope="col"> Detalle</th>*@
                    <th scope="col"> Curriculum</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Count; i++)
                {
                    <tr>
                        <th scope="row"> @(i+1) </th>
                        <td> @Model[i].Nombre </td>
                        <td> @Model[i].Apellidos </td>
                        <td> @Model[i].Email </td>
                        <td> 
                            <form method="post" asp-action="EliminarCurriculum">
                                    <button value="@Model[i].Id" name="id" type="submit" class="btn btn-danger w-50">
                                        <i class="bi bi-trash"></i>
                                    </button>
                            </form>
                        </td>
                        <td>
                             <form method="post" asp-action="ActualizarCurriculum">
                                    <button value="@Model[i].Id" name="id" type="submit" class="btn btn-info  w-50">
                                        <i class="bi bi-pencil"></i>
                                    </button>
                             </form>
                        </td>
                      @*  <td>
                              <button class="d-block btn btn-warning mt-2">
                                    <a href="/Validaciones/DetallePorId/@Model[i].Id">
                                        <i class="bi bi-info-circle"></i>
                                    </a>
                                </button>
                        </td>*@
                        <td>
                            <button class="d-block btn btn-info  w-50">
                                    <a href="/Validaciones/CurriculumPorId/@Model[i].Id">
                                        <i class="bi bi-eye text-white"></i>
                                    </a>
                            </button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
            </div>
        </div>
     </article>
</section>

                @*                <style>
    .custom-css
    {
        display:grid;
        grid-template-columns: repeat(2,1fr);
    }
                                </style>*@